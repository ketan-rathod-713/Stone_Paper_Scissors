// how backend will look like

class Room{
    constructor(){
        // get room id
        // get user1 name
        // roomEmpty: true
        // privateRoom: true, only on invite basis
    }

    joinRoom = () => {

    }

    exitRoom = ()=>{
        
    }

    chooseOptions = ()=>{ // when user chooses one of the three options displayed it can be 3 or choose none

    }

    // messaging functionality if one user sends then let other recieve and so on

    message = ()=>{ // let one user message other user, it will not be stored.

    }
}